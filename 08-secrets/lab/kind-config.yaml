# One control-plane and one worker is plenty. Secrets sync has no AZ or
# multi-node story, so the cluster stays small.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: secrets-lab
nodes:
  - role: control-plane
  - role: worker
