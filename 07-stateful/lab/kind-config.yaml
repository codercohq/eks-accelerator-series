# Three nodes so a StatefulSet pod can be moved between them, the local stand-in
# for Availability Zones. We label the two workers as fake zones in the lab.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: stateful-lab
nodes:
  - role: control-plane
  - role: worker
  - role: worker
