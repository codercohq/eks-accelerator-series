apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: worker
  template:
    metadata:
      labels:
        app: worker
    spec:
      containers:
        - name: app
          image: worker:latest        # overlays set the real registry + tag
          envFrom:
            - configMapRef:
                name: app-config
          # requests are what the scheduler packs on. limits cap the noisy neighbour.
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              memory: 256Mi
          # no HTTP port, so use a process check for liveness.
          livenessProbe:
            exec:
              command: ["sh", "-c", "pgrep app || exit 1"]
            initialDelaySeconds: 15
            periodSeconds: 20
